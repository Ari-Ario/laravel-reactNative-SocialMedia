/**
 * PlatformCameraView.tsx
 * Type-only barrel for TypeScript resolution.
 * Metro will pick PlatformCameraView.native.tsx or PlatformCameraView.web.tsx at runtime.
 */
export { default } from './PlatformCameraView.native';
