name: Deploy to Forge

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Forge Deployment
        run: |
          curl -X GET "${{ secrets.FORGE_DEPLOYMENT_TRIGGER_URL }}"
